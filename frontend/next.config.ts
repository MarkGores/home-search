import { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["mls-grid-photos-scott-county.s3.us-east-2.amazonaws.com"],
  },
  // other configuration options...
};

export default nextConfig;